name: crystal_lambda
version: 0.1.0

authors:
  - Steffan Perry <sperry1988@gmail.com>

description: |
  Custom lambda runtime for Crystal
license: MIT-0

dependencies:
  dotenv:
    github: drum445/dotenv

targets:
  example:
    main: src/example.cr
